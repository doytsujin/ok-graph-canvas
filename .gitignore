dist/
*.tgz
node_modules/
.canvas-check.cjs
.filter-check.cjs
.trace-check.mjs
examples/dist/
examples/node_modules/
# Cargo puts target/ next to each crate, not at the workspace root, so the
# pattern has to be a glob. `rust/target` matched nothing and 184 build
# artifacts went into a commit.
rust/**/target/
